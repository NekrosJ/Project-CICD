variables:
  IMAGE_NAME: nekrosj47/projectcicd
  IMAGE_TAG: latest

stages:
  - install
  - build
  - test
  - deploy

# Job to install dependencies
install:
  stage: install
  image: node:16
  script:
    - npm install
  artifacts:
    paths:
      - node_modules/

# Job to build the application and Docker image
build:
  stage: build
  image: docker:20.10.16 # Docker image with Docker installed
  services:
    - docker:20.10.16-dind # Docker-in-Docker service
  variables:
    DOCKER_TLS_CERTDIR: "/certs"
  before_script:
    - docker login -u $REGISTRY_USER -p $REGISTRY_PASS
  script:
    - docker build -t $IMAGE_NAME:$IMAGE_TAG .
    - docker push $IMAGE_NAME:$IMAGE_TAG

# Job to run tests  
test:
  stage: test
  image: node:16
  script:                                                     # Running tests
    - npm run test
  dependencies:
    - install

# # Job to deploy the application
# deploy:
#   stage: deploy
#   before_script:
#     - chmod 400 $SSH_KEY
#   script:
#     - ssh -o StrictHostKeyChecking=no -i $SSH_KEY root@161.35.223.117 "
#         docker login -u $REGISTRY_USER -p $REGISTRY_PASS &&
#         docker ps -aq | xargs docker stop | xargs docker rm &&
#         docker run -d -p 5000:5000 $IMAGE_NAME:$IMAGE_TAG"
